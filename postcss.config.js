module.exports = {
  plugins: {
    // The configurations here can be built out better
    // This is where we can target specific browser versions and optimization features
    'postcss-import': {},
    'postcss-preset-env': {},
    'cssnano': {}
  }
};
